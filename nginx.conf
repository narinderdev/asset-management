server {
    listen 80;
    listen [::]:80;

    root /usr/share/nginx/html;
    index index.csr.html index.html;

    server_name _;

    location / {
        try_files $uri $uri/ /index.csr.html;
    }
}